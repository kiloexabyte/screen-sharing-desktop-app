name: On Push To Main

on:
  push:
    branches:
      - main
    paths-ignore:
      - "**.md"

jobs:
  lint:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/kiloexabyte/wails-ubuntu-amd64
    steps:
      - uses: actions/checkout@v6

      - name: Cache node modules
        uses: actions/cache@v4
        with:
          path: ./frontend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Run CI
        env:
          GOFLAGS: -buildvcs=false
        run: op ci

  build-linux-windows:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/kiloexabyte/wails-ubuntu-amd64
    steps:
      - uses: actions/checkout@v6

      - name: Cache node modules
        uses: actions/cache@v4
        with:
          path: ./frontend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Build linux/amd64 and windows/amd64
        env:
          GOFLAGS: -buildvcs=false
          PLATFORMS: linux/amd64,linux/386,windows/amd64,windows/386
        run: op build_all

  build-linux-arm64:
    runs-on: ubuntu-24.04-arm
    container:
      image: ghcr.io/kiloexabyte/wails-ubuntu-arm64
    steps:
      - uses: actions/checkout@v6

      - name: Cache node modules
        uses: actions/cache@v4
        with:
          path: ./frontend/node_modules
          key: ${{ runner.os }}-arm64-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-arm64-node-

      - name: Build linux/arm64
        env:
          GOFLAGS: -buildvcs=false
          PLATFORMS: linux/arm64
          WAILS_TAGS: webkit2_41
        run: op build_all

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v5
        with:
          go-version: "1.24"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Go tools
        run: |
          go install github.com/wailsapp/wails/v2/cmd/wails@latest
          go install lesiw.io/op@latest

      - name: Build darwin/amd64 and darwin/arm64
        env:
          PLATFORMS: darwin/amd64,darwin/arm64
        run: op build_all
